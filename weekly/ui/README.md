# MyMemory weekly UI

Human review UI for weekly staging. Optional. The provider works without it.

```bash
npm install
npm run dev
```

Or run `/weekly ui` from Hermes chat after MyMemory is enabled. Do not commit `.env` or live staging files.
